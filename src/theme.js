const baseFontSize = 16;
export default {
	headerHeight: `${baseFontSize * 3.5}px`,
	headerInnerHeight: '36px',
	baseFontSize: `${baseFontSize}px`,
	halfFontSize: `${baseFontSize / 2}px`,
	sidebarWidth: `${baseFontSize * 16}px`,
	color: {
		primary: 'indigo',
		primaryText: 'white',
	},
	size: {
		container: '1024px',
	},
	border: {},
	borderColor: {},
};
